# Basic direnv configuration for Nix flakes
# Place this file in your project root as .envrc

# Use the default dev shell
use flake

# Or use a specific dev shell:
# use flake .#python
# use flake .#rust
# use flake .#node
# use flake .#go
# use flake .#cpp
# use flake .#web

# After creating/editing .envrc, run:
#   direnv allow
